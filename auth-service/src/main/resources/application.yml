server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://localhost:5432/ventry
    username: ventry
    password: ventry
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: auth

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true

jwt:
  secret: ${JWT_SECRET:pfz/srQh1hqvsV8yHttL23iKS+WGfhd3cLJ54AvATNs=}
  expiration-ms: ${JWT_EXPIRATION_MS:3600000}
